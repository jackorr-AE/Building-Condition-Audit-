Place project assets here:

- `logos/` — client and engineer logo PNGs (paths configured in config/project.json)
- `photos/` — Fulcrum photo files named by GUID or device filename (e.g. `{uuid}.jpg`)
